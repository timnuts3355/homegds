# リリース前総合確認（2026-09-22）

## 結果

ローカルの独立した検証環境（localhost:3004）で確認。本番データは変更していません。

確認で再現し、修正した不具合：

- Home・在庫で、pointercancelがスワイプ確定として処理され、数量変更や遷移が起きる。
- Home・在庫で、縦方向の移動がタップと判定される。
- 在庫の長押しメニューを開いた後、指を離すと商品詳細へ遷移する。
- CSVに含まれる未登録の同名商品が、重複確認なしで二重登録される。
- CSVの数量・最低在庫でInfinity・桁あふれ・空欄が受け入れられる。

## 検証

- `node tests/release-regressions.cjs`：12件成功。修正前には10件中7件が失敗し、修正後に再現ケースの成功と追加2件の成功を確認。
- `npm run build`：型確認・lintを含め成功。
- `git diff --check`：成功。
- CSV：ファイル内の重複検出、個別上書き、不正数値のスキップ、取り込み結果表示を実画面で確認。結果は新規2件、上書き1件、エラー1件。
- CSV書き出し：ダウンロード通知はブラウザツールが取得できなかったが、保存済みCSVの存在・時刻・2商品の内容を確認。引用符・改行を含む名称の往復変換は回帰テストで確認。
- 在庫：左右のドラッグで数量が1→2→1と変化し、画面遷移なし。縦ドラッグでは数量変更・遷移なし。
- Home：右スワイプで1→2に補充し、在庫不足から在庫が少ないセクションへ移動。詳細への誤遷移なし。
- お気に入り：在庫画面に留まって切り替え可能。
- 商品：名称編集、保存、Homeへの戻り、繁体字での追加・削除、削除後の在庫への復帰を確認。
- 言語：日本語→繁体字へ切り替え後、統計と戻る操作でzh-TWを維持。
- 統計：食品1件・飲料1件を50%ずつ表示。
- 履歴：追加、上書き、編集、使用、補充の記録と単位翻訳を確認。
- ブラウザコンソール：確認タブに警告・エラーなし。

スワイプ中断・長押しは実際のイベントハンドラを呼ぶ回帰テストで確認。iPhone実機でのタッチ操作は今回の確認に含まれません。

期限管理などの新機能、無関係な.gitignore変更は含めていません。
